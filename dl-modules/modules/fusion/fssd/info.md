# FSSD

**论文**: [arxiv.org](https://arxiv.org/pdf/1712.00960v4)

## 模块简介

本文提出特征融合FSSD，这是一种改进的特征融合算法，采用了一种新型的、轻量级的特征融合模块，可以在速度稍有下降的情况下显著提高SSD的性能。特征融合模块，将不同层的不同尺度的特征拼接在一起，然后通过一些下采样块生成新的特征金字塔，并将新特征金字塔反馈给多盒检测器，预测最终的检测结果。
